# Note: Dependencies are managed via pyproject.toml
# This file is kept for reference only
# Use 'uv' for building: uv pip install --python 3.12 --target <dir> requests boto3

requests>=2.31.0
boto3>=1.34.0
